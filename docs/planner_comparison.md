# Classical planner comparison

Auto-generated by `nav2_planner_benchmarks` (`ros2 run nav2_planner_benchmarks planner_benchmark`). All planners run in-process against a live `nav2_costmap_2d::Costmap2DROS` (6x6 m, 0.05 m resolution, no Gazebo/GPU). Times are the median of 21 plans on the dev machine and are indicative only (absolute numbers vary with load); compare relative magnitudes and the path-length / shape columns.

Planners (all `nav2_core::GlobalPlanner` plugins absent from upstream Nav2 — eight classical plus three generative Mode B variants: analytic, learned, and learned+classical hybrid):

- **RRT*** — sampling (optimal)
- **RRT-Connect** — sampling (bidirectional)
- **PRM** — sampling (roadmap)
- **D* Lite** — incremental search
- **JPS** — grid A* speed-up
- **Lazy Theta*** — any-angle
- **ARA*** — anytime
- **Visibility graph** — geometric
- **Diffusion (Mode B, analytic)** — generative fan (propose + validate)
- **Diffusion (Mode B, learned)** — generative flow + costmap (propose + validate)
- **Diffusion (Mode B, hybrid)** — generative propose + classical (JPS) fallback

> D\* Lite caches its goal-rooted search across calls, so its median reflects warm incremental replans (the first cold plan is slower). The others replan from scratch each call.

> **Diffusion (Mode B)** is the generative planner — a model *proposes* candidate paths and the deterministic validity layer *disposes* of colliding ones, keeping the shortest survivor. Three variants run here. **analytic** uses the built-in `FanPathModel` (a symmetric bowed fan, no ONNX); **learned** loads the curated costmap-conditioned flow model from `model_zoo` via `OnnxPathModel` (real ONNX inference). Both are pure generative, so unlike the search planners they are not complete: if no proposal threads the gap they report no path. The learned model reads the costmap and biases every proposal to the open side (see its model card), but its synthetic training distribution caps the detour size — so it clears *clear* and *side obstacle* yet cannot make the 2 m swing of *off-centre gap* or the S of *slalom*. The **hybrid** variant keeps the learned proposal but adds a classical (JPS) fallback: when no proposal threads the map it hands off to a complete search, so it solves every scenario while still using the fast generative path on the easy ones. This is the propose/dispose split taken one step further — learned proposes, classical search disposes (see docs/generative_limits.md).

## Scenario: clear

Empty 6x6 m map, off-axis goal. Start (1, 1) -> goal (5, 5).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 5.658 | 10 | 953.556 |
| RRT-Connect | sampling (bidirectional) | yes | 5.657 | 13 | 0.040 |
| PRM | sampling (roadmap) | yes | 6.175 | 17 | 23.664 |
| D* Lite | incremental search | yes | 5.657 | 81 | 0.528 |
| JPS | grid A* speed-up | yes | 5.657 | 81 | 0.811 |
| Lazy Theta* | any-angle | yes | 5.657 | 115 | 0.650 |
| ARA* | anytime | yes | 5.657 | 81 | 1.725 |
| Visibility graph | geometric | yes | 5.657 | 115 | 0.636 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 5.657 | 40 | 0.137 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | yes | 5.669 | 12 | 0.227 |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 5.669 | 12 | 0.389 |

## Scenario: off-centre gap

Vertical wall with a gap well off the straight line. Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 5.084 | 12 | 717.221 |
| RRT-Connect | sampling (bidirectional) | yes | 6.359 | 14 | 0.105 |
| PRM | sampling (roadmap) | yes | 5.300 | 14 | 30.157 |
| D* Lite | incremental search | yes | 5.257 | 81 | 0.530 |
| JPS | grid A* speed-up | yes | 5.278 | 81 | 0.838 |
| Lazy Theta* | any-angle | yes | 5.073 | 104 | 8.085 |
| ARA* | anytime | yes | 5.278 | 81 | 17.123 |
| Visibility graph | geometric | yes | 5.095 | 105 | 0.962 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 5.282 | 40 | 0.111 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 5.278 | 81 | 1.255 |

## Scenario: slalom

Two staggered walls (gap low then high) forcing an S-shaped detour. Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 6.615 | 13 | 647.390 |
| RRT-Connect | sampling (bidirectional) | yes | 10.159 | 22 | 0.585 |
| PRM | sampling (roadmap) | yes | 8.058 | 23 | 29.242 |
| D* Lite | incremental search | yes | 6.785 | 107 | 0.564 |
| JPS | grid A* speed-up | yes | 6.829 | 107 | 0.817 |
| Lazy Theta* | any-angle | yes | 6.534 | 137 | 18.358 |
| ARA* | anytime | yes | 6.808 | 107 | 36.104 |
| Visibility graph | geometric | yes | 6.610 | 136 | 0.988 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 6.829 | 107 | 1.257 |

## Scenario: side obstacle

One-sided block across the straight line; a small detour to the open side clears it (matches the learned model's competence). Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 4.028 | 9 | 711.076 |
| RRT-Connect | sampling (bidirectional) | yes | 5.395 | 12 | 0.062 |
| PRM | sampling (roadmap) | yes | 4.217 | 12 | 29.354 |
| D* Lite | incremental search | yes | 4.180 | 81 | 0.537 |
| JPS | grid A* speed-up | yes | 4.201 | 81 | 0.810 |
| Lazy Theta* | any-angle | yes | 4.027 | 82 | 1.382 |
| ARA* | anytime | yes | 4.201 | 81 | 3.216 |
| Visibility graph | geometric | yes | 4.029 | 82 | 0.899 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 4.097 | 40 | 0.116 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | yes | 4.076 | 12 | 0.390 |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 4.076 | 12 | 0.380 |

_Path length is the sum of consecutive pose distances; lower is shorter (not always better — sampling planners trade optimality for speed, ARA\* trades it for an anytime bound). Pose count reflects densification granularity. "no path" means the planner raised an exception (e.g. the sampling budget did not connect start and goal within the scenario)._
