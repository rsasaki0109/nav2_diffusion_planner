# Classical planner comparison

Auto-generated by `nav2_planner_benchmarks` (`ros2 run nav2_planner_benchmarks planner_benchmark`). All planners run in-process against a live `nav2_costmap_2d::Costmap2DROS` (6x6 m, 0.05 m resolution, no Gazebo/GPU). Times are the median of 21 plans on the dev machine and are indicative only (absolute numbers vary with load); compare relative magnitudes and the path-length / shape columns.

Planners (all `nav2_core::GlobalPlanner` plugins absent from upstream Nav2 — eight classical plus ten generative Mode B variants: analytic, learned (flow), learned (transformer), learned (recurrent), learned (attnseq), three kinematics-conditioned (omni / diff / Ackermann), learned+classical fallback hybrid, and guided hybrid):

- **RRT*** — sampling (optimal)
- **RRT-Connect** — sampling (bidirectional)
- **PRM** — sampling (roadmap)
- **D* Lite** — incremental search
- **JPS** — grid A* speed-up
- **Lazy Theta*** — any-angle
- **ARA*** — anytime
- **Visibility graph** — geometric
- **Diffusion (Mode B, analytic)** — generative fan (propose + validate)
- **Diffusion (Mode B, learned)** — generative flow + costmap (propose + validate)
- **Diffusion (Mode B, transformer)** — generative transformer + costmap (footprint-aware; threads off-centre + dead-ahead gaps)
- **Diffusion (Mode B, recurrent)** — generative GRU rollout + costmap (fanned proposals)
- **Diffusion (Mode B, attnseq)** — generative cross-attn autoregressive + costmap (no-fan; threads all 8 courses incl. slalom + far off-centre)
- **Diffusion (Mode B, kinematics omni)** — kinematics-conditioned (R=0, omni-directional) + curvature validator off (threads all 8 courses incl. slalom)
- **Diffusion (Mode B, kinematics diff)** — kinematics-conditioned (R=0.3, diff-drive) + curvature validator
- **Diffusion (Mode B, kinematics Ackermann)** — kinematics-conditioned (R=1.5, Ackermann) + curvature validator disposes infeasible turns
- **Diffusion (Mode B, hybrid)** — generative propose + classical (JPS) fallback
- **Diffusion (Mode B, guided)** — generative-guided complete A* (tightly coupled)

> D\* Lite caches its goal-rooted search across calls, so its median reflects warm incremental replans (the first cold plan is slower). The others replan from scratch each call.

> **Diffusion (Mode B)** is the generative planner — a model *proposes* candidate paths and the deterministic validity layer *disposes* of colliding ones, keeping the shortest survivor. Ten variants run here. **analytic** uses the built-in `FanPathModel` (a symmetric bowed fan, no ONNX); **learned (flow)** loads the curated costmap-conditioned flow model from `model_zoo` via `OnnxPathModel` (real ONNX inference); **learned (transformer)** loads the transformer Mode B model (attention over costmap tokens); **learned (recurrent)** loads the GRU-rollout Mode B model (a third family that emits each path one waypoint at a time); **learned (attnseq)** loads the no-fan cross-attention autoregressive model (a fourth family). All are pure generative, so unlike the search planners they are not complete: if no proposal threads the gap they report no path. The learned families split the scenarios along a clear axis. The flow and recurrent models read the costmap and bias proposals to the open side: they thread every gap that sits **dead ahead** (*centred gap*, *narrow gap*, *double gate*) and clear *side obstacle*, but their 16-d CNN embedding can only pick a free *side* — it cannot localize a slot that is swung off-axis, so they fail *off-centre gap* and *far off-centre gap*. The transformer goes further: attention over explicit costmap tokens lets it *aim* at an off-axis slot, and a **differentiable footprint-clearance loss** pulls each proposal's wall crossing into the free slot with margin — so it **threads the footprint-validated *off-centre gap*** plus the *centred*/*narrow*/*double gate* dead-ahead gaps and *side obstacle* (6/8); its bounds are the *far off-centre gap* and the S-shaped *slalom*. The **attnseq** family clears both: it drops the lateral fan (K learned seeds let a candidate take an *S* shape, which a uniformly shifted fan cannot) and reads the costmap at every rollout step. Trained on collision-clean **plateau** experts with **deployment-matched** patches — the two fixes that turned *slalom* and *far off-centre* from apparent architecture ceilings into threadable courses (they were a colliding training expert and a train/inference patch mismatch, not a model limit) — it **threads all eight courses** as a pure-generative proposer, the first Mode B model here to clear the slalom and the far off-centre gap without a classical fallback (see docs/generative_limits.md). The three **kinematics** variants show the seam carries vehicle dynamics too: *one* model, conditioned on the commanded min turn radius R (the second context input, 0 = omni-directional), proposes a sharp detour for an omni / differential-drive robot (R=0 / R=0.3) but a gentle one for an Ackermann car (R=1.5) through the *same* gap, and the planner's **curvature validator disposes** any proposal that turns tighter than 1/R. The **omni** row has the validator off (R=0) and threads all eight courses including the S-shaped *slalom* (its +/-2 m crossings in a ~1.6 m gap need a near-pivot only an omni robot has); the **diff** row (R=0.3, gate 1/R=3.33) also threads all eight -- its sharp turning circle clears every lateral maneuver and the coarse 12-pose slalom S stays under its gate; the **Ackermann** row (R=1.5, gate 1/R=0.67) threads only the near-straight courses (*clear*, *centred gap*, *double gate*) and the curvature validator reports *no path* on every course that needs a real lateral move -- *narrow gap* (a ~1.5 aim correction into the tight slot), *off-centre* / *far off-centre gap* (a 2 m jog in ~1 m of travel), *side obstacle* (a ~2.2 bow), and *slalom* -- all past a 1.5 m turning circle. All three are the same weights, differing only in the R input and the curvature gate (omni 8/8, diff 8/8, Ackermann 3/8). The **hybrid** variant keeps the learned proposal but adds a classical (JPS) fallback: when no proposal threads the map it hands off to a complete search, so it solves every scenario while still using the fast generative path on the easy ones. This is the propose/dispose split taken one step further — learned proposes, classical search disposes. The **hybrid (guided)** variant couples them tightly: it always runs a complete A* but discounts cell costs near the valid proposals, so the learned model *shapes* every route while the search guarantees completeness — note its cell-resolution pose counts even on easy maps, where the fallback hybrid instead returns the 12-pose generative path (see docs/generative_limits.md).

## Scenario: clear

Empty 6x6 m map, off-axis goal. Start (1, 1) -> goal (5, 5).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 5.658 | 10 | 425.016 |
| RRT-Connect | sampling (bidirectional) | yes | 5.657 | 13 | 0.021 |
| PRM | sampling (roadmap) | yes | 6.175 | 17 | 15.970 |
| D* Lite | incremental search | yes | 5.657 | 81 | 0.302 |
| JPS | grid A* speed-up | yes | 5.657 | 81 | 0.425 |
| Lazy Theta* | any-angle | yes | 5.657 | 115 | 0.354 |
| ARA* | anytime | yes | 5.657 | 81 | 1.189 |
| Visibility graph | geometric | yes | 5.657 | 115 | 0.478 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 5.657 | 40 | 0.069 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | yes | 5.669 | 12 | 0.170 |
| Diffusion (Mode B, transformer) | generative transformer + costmap (footprint-aware; threads off-centre + dead-ahead gaps) | yes | 5.658 | 12 | 0.189 |
| Diffusion (Mode B, recurrent) | generative GRU rollout + costmap (fanned proposals) | yes | 5.657 | 12 | 0.660 |
| Diffusion (Mode B, attnseq) | generative cross-attn autoregressive + costmap (no-fan; threads all 8 courses incl. slalom + far off-centre) | yes | 5.660 | 12 | 4.646 |
| Diffusion (Mode B, kinematics omni) | kinematics-conditioned (R=0, omni-directional) + curvature validator off (threads all 8 courses incl. slalom) | yes | 5.658 | 12 | 3.433 |
| Diffusion (Mode B, kinematics diff) | kinematics-conditioned (R=0.3, diff-drive) + curvature validator | yes | 5.657 | 12 | 3.795 |
| Diffusion (Mode B, kinematics Ackermann) | kinematics-conditioned (R=1.5, Ackermann) + curvature validator disposes infeasible turns | yes | 5.657 | 12 | 3.705 |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 5.669 | 12 | 0.163 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 5.657 | 81 | 0.277 |

## Scenario: centred gap

Vertical wall with a gap centred on the straight line; the gap sits dead ahead so no off-axis localization is needed (isolates threading ability from slot localization). Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 4.000 | 9 | 399.107 |
| RRT-Connect | sampling (bidirectional) | yes | 4.064 | 10 | 0.019 |
| PRM | sampling (roadmap) | yes | 4.200 | 13 | 15.528 |
| D* Lite | incremental search | yes | 4.014 | 81 | 0.301 |
| JPS | grid A* speed-up | yes | 4.014 | 81 | 0.229 |
| Lazy Theta* | any-angle | yes | 4.000 | 81 | 0.290 |
| ARA* | anytime | yes | 4.014 | 81 | 0.796 |
| Visibility graph | geometric | yes | 4.000 | 81 | 0.461 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 4.000 | 40 | 0.052 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | yes | 4.011 | 12 | 0.164 |
| Diffusion (Mode B, transformer) | generative transformer + costmap (footprint-aware; threads off-centre + dead-ahead gaps) | yes | 4.031 | 12 | 0.169 |
| Diffusion (Mode B, recurrent) | generative GRU rollout + costmap (fanned proposals) | yes | 4.048 | 12 | 0.658 |
| Diffusion (Mode B, attnseq) | generative cross-attn autoregressive + costmap (no-fan; threads all 8 courses incl. slalom + far off-centre) | yes | 4.001 | 12 | 3.369 |
| Diffusion (Mode B, kinematics omni) | kinematics-conditioned (R=0, omni-directional) + curvature validator off (threads all 8 courses incl. slalom) | yes | 4.000 | 12 | 3.444 |
| Diffusion (Mode B, kinematics diff) | kinematics-conditioned (R=0.3, diff-drive) + curvature validator | yes | 4.000 | 12 | 3.359 |
| Diffusion (Mode B, kinematics Ackermann) | kinematics-conditioned (R=1.5, Ackermann) + curvature validator disposes infeasible turns | yes | 4.001 | 12 | 3.457 |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 4.011 | 12 | 0.168 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 4.014 | 81 | 0.257 |

## Scenario: narrow gap

Vertical wall with a tight gap centred on the line; halves the clearance of *centred gap* to probe the footprint margin. Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 4.000 | 9 | 399.878 |
| RRT-Connect | sampling (bidirectional) | yes | 4.064 | 10 | 0.019 |
| PRM | sampling (roadmap) | yes | 4.200 | 13 | 15.584 |
| D* Lite | incremental search | yes | 4.014 | 81 | 0.290 |
| JPS | grid A* speed-up | yes | 4.014 | 81 | 0.182 |
| Lazy Theta* | any-angle | yes | 4.000 | 81 | 0.283 |
| ARA* | anytime | yes | 4.014 | 81 | 0.788 |
| Visibility graph | geometric | yes | 4.000 | 81 | 0.459 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 4.000 | 40 | 0.049 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | yes | 4.012 | 12 | 0.168 |
| Diffusion (Mode B, transformer) | generative transformer + costmap (footprint-aware; threads off-centre + dead-ahead gaps) | yes | 4.029 | 12 | 0.179 |
| Diffusion (Mode B, recurrent) | generative GRU rollout + costmap (fanned proposals) | yes | 4.081 | 12 | 0.665 |
| Diffusion (Mode B, attnseq) | generative cross-attn autoregressive + costmap (no-fan; threads all 8 courses incl. slalom + far off-centre) | yes | 4.001 | 12 | 3.353 |
| Diffusion (Mode B, kinematics omni) | kinematics-conditioned (R=0, omni-directional) + curvature validator off (threads all 8 courses incl. slalom) | yes | 4.036 | 12 | 3.375 |
| Diffusion (Mode B, kinematics diff) | kinematics-conditioned (R=0.3, diff-drive) + curvature validator | yes | 4.055 | 12 | 3.484 |
| Diffusion (Mode B, kinematics Ackermann) | kinematics-conditioned (R=1.5, Ackermann) + curvature validator disposes infeasible turns | no path | — | — | — |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 4.012 | 12 | 0.162 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 4.014 | 81 | 0.246 |

## Scenario: off-centre gap

Vertical wall with a gap well off the straight line. Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 5.084 | 12 | 399.614 |
| RRT-Connect | sampling (bidirectional) | yes | 6.359 | 14 | 0.056 |
| PRM | sampling (roadmap) | yes | 5.300 | 14 | 16.191 |
| D* Lite | incremental search | yes | 5.257 | 81 | 0.303 |
| JPS | grid A* speed-up | yes | 5.278 | 81 | 0.440 |
| Lazy Theta* | any-angle | yes | 5.073 | 104 | 4.355 |
| ARA* | anytime | yes | 5.278 | 81 | 9.127 |
| Visibility graph | geometric | yes | 5.095 | 105 | 0.494 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 5.282 | 40 | 0.053 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, transformer) | generative transformer + costmap (footprint-aware; threads off-centre + dead-ahead gaps) | yes | 6.082 | 12 | 0.177 |
| Diffusion (Mode B, recurrent) | generative GRU rollout + costmap (fanned proposals) | no path | — | — | — |
| Diffusion (Mode B, attnseq) | generative cross-attn autoregressive + costmap (no-fan; threads all 8 courses incl. slalom + far off-centre) | yes | 6.422 | 12 | 3.452 |
| Diffusion (Mode B, kinematics omni) | kinematics-conditioned (R=0, omni-directional) + curvature validator off (threads all 8 courses incl. slalom) | yes | 5.890 | 12 | 3.556 |
| Diffusion (Mode B, kinematics diff) | kinematics-conditioned (R=0.3, diff-drive) + curvature validator | yes | 5.842 | 12 | 3.349 |
| Diffusion (Mode B, kinematics Ackermann) | kinematics-conditioned (R=1.5, Ackermann) + curvature validator disposes infeasible turns | no path | — | — | — |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 5.278 | 81 | 0.607 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 5.257 | 81 | 0.529 |

## Scenario: far off-centre gap

Same 2 m off-axis gap but pushed toward the goal (world_x 4.0); probes whether gap threading holds when the wall sits beyond the learned forward span (~2 m aligned). Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 5.284 | 12 | 397.171 |
| RRT-Connect | sampling (bidirectional) | yes | 6.938 | 16 | 0.063 |
| PRM | sampling (roadmap) | yes | 5.540 | 17 | 15.526 |
| D* Lite | incremental search | yes | 5.638 | 94 | 0.313 |
| JPS | grid A* speed-up | yes | 5.659 | 94 | 0.431 |
| Lazy Theta* | any-angle | yes | 5.254 | 107 | 7.732 |
| ARA* | anytime | yes | 5.659 | 94 | 16.515 |
| Visibility graph | geometric | yes | 5.288 | 108 | 0.483 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, transformer) | generative transformer + costmap (footprint-aware; threads off-centre + dead-ahead gaps) | no path | — | — | — |
| Diffusion (Mode B, recurrent) | generative GRU rollout + costmap (fanned proposals) | no path | — | — | — |
| Diffusion (Mode B, attnseq) | generative cross-attn autoregressive + costmap (no-fan; threads all 8 courses incl. slalom + far off-centre) | yes | 6.702 | 12 | 3.368 |
| Diffusion (Mode B, kinematics omni) | kinematics-conditioned (R=0, omni-directional) + curvature validator off (threads all 8 courses incl. slalom) | yes | 6.074 | 12 | 3.760 |
| Diffusion (Mode B, kinematics diff) | kinematics-conditioned (R=0.3, diff-drive) + curvature validator | yes | 6.148 | 12 | 3.902 |
| Diffusion (Mode B, kinematics Ackermann) | kinematics-conditioned (R=1.5, Ackermann) + curvature validator disposes infeasible turns | no path | — | — | — |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 5.659 | 94 | 0.613 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 5.667 | 95 | 0.895 |

## Scenario: double gate

Two vertical walls in series, both gaps on the straight line; two sequential crossings without an S-detour (between *side obstacle* and *slalom* in difficulty). Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 4.000 | 9 | 390.992 |
| RRT-Connect | sampling (bidirectional) | yes | 4.064 | 10 | 0.019 |
| PRM | sampling (roadmap) | yes | 4.179 | 13 | 15.527 |
| D* Lite | incremental search | yes | 4.014 | 81 | 0.290 |
| JPS | grid A* speed-up | yes | 4.014 | 81 | 0.223 |
| Lazy Theta* | any-angle | yes | 4.000 | 81 | 0.283 |
| ARA* | anytime | yes | 4.014 | 81 | 0.792 |
| Visibility graph | geometric | yes | 4.000 | 81 | 0.482 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 4.000 | 40 | 0.050 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | yes | 4.059 | 12 | 0.170 |
| Diffusion (Mode B, transformer) | generative transformer + costmap (footprint-aware; threads off-centre + dead-ahead gaps) | yes | 4.123 | 12 | 0.172 |
| Diffusion (Mode B, recurrent) | generative GRU rollout + costmap (fanned proposals) | yes | 4.253 | 12 | 1.130 |
| Diffusion (Mode B, attnseq) | generative cross-attn autoregressive + costmap (no-fan; threads all 8 courses incl. slalom + far off-centre) | yes | 4.001 | 12 | 3.484 |
| Diffusion (Mode B, kinematics omni) | kinematics-conditioned (R=0, omni-directional) + curvature validator off (threads all 8 courses incl. slalom) | yes | 4.000 | 12 | 3.271 |
| Diffusion (Mode B, kinematics diff) | kinematics-conditioned (R=0.3, diff-drive) + curvature validator | yes | 4.000 | 12 | 3.404 |
| Diffusion (Mode B, kinematics Ackermann) | kinematics-conditioned (R=1.5, Ackermann) + curvature validator disposes infeasible turns | yes | 4.000 | 12 | 3.463 |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 4.059 | 12 | 0.170 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 4.014 | 81 | 0.254 |

## Scenario: slalom

Two staggered walls (gap low then high) forcing an S-shaped detour. Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 6.615 | 13 | 358.228 |
| RRT-Connect | sampling (bidirectional) | yes | 10.159 | 22 | 0.310 |
| PRM | sampling (roadmap) | yes | 8.058 | 23 | 15.682 |
| D* Lite | incremental search | yes | 6.785 | 107 | 0.328 |
| JPS | grid A* speed-up | yes | 6.829 | 107 | 0.440 |
| Lazy Theta* | any-angle | yes | 6.534 | 137 | 9.773 |
| ARA* | anytime | yes | 6.808 | 107 | 18.983 |
| Visibility graph | geometric | yes | 6.610 | 136 | 0.495 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, transformer) | generative transformer + costmap (footprint-aware; threads off-centre + dead-ahead gaps) | no path | — | — | — |
| Diffusion (Mode B, recurrent) | generative GRU rollout + costmap (fanned proposals) | no path | — | — | — |
| Diffusion (Mode B, attnseq) | generative cross-attn autoregressive + costmap (no-fan; threads all 8 courses incl. slalom + far off-centre) | yes | 9.900 | 12 | 3.373 |
| Diffusion (Mode B, kinematics omni) | kinematics-conditioned (R=0, omni-directional) + curvature validator off (threads all 8 courses incl. slalom) | yes | 9.982 | 12 | 4.762 |
| Diffusion (Mode B, kinematics diff) | kinematics-conditioned (R=0.3, diff-drive) + curvature validator | yes | 9.777 | 12 | 3.359 |
| Diffusion (Mode B, kinematics Ackermann) | kinematics-conditioned (R=1.5, Ackermann) + curvature validator disposes infeasible turns | no path | — | — | — |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 6.829 | 107 | 0.614 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 6.925 | 111 | 1.128 |

## Scenario: side obstacle

One-sided block across the straight line; a small detour to the open side clears it (matches the learned model's competence). Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 4.028 | 9 | 394.126 |
| RRT-Connect | sampling (bidirectional) | yes | 5.395 | 12 | 0.032 |
| PRM | sampling (roadmap) | yes | 4.217 | 12 | 15.822 |
| D* Lite | incremental search | yes | 4.180 | 81 | 0.300 |
| JPS | grid A* speed-up | yes | 4.201 | 81 | 0.486 |
| Lazy Theta* | any-angle | yes | 4.027 | 82 | 0.711 |
| ARA* | anytime | yes | 4.201 | 81 | 1.683 |
| Visibility graph | geometric | yes | 4.029 | 82 | 0.458 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 4.097 | 40 | 0.056 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | yes | 4.076 | 12 | 0.164 |
| Diffusion (Mode B, transformer) | generative transformer + costmap (footprint-aware; threads off-centre + dead-ahead gaps) | yes | 4.872 | 12 | 0.170 |
| Diffusion (Mode B, recurrent) | generative GRU rollout + costmap (fanned proposals) | yes | 4.246 | 12 | 0.653 |
| Diffusion (Mode B, attnseq) | generative cross-attn autoregressive + costmap (no-fan; threads all 8 courses incl. slalom + far off-centre) | yes | 5.395 | 12 | 3.459 |
| Diffusion (Mode B, kinematics omni) | kinematics-conditioned (R=0, omni-directional) + curvature validator off (threads all 8 courses incl. slalom) | yes | 4.980 | 12 | 3.259 |
| Diffusion (Mode B, kinematics diff) | kinematics-conditioned (R=0.3, diff-drive) + curvature validator | yes | 4.906 | 12 | 3.458 |
| Diffusion (Mode B, kinematics Ackermann) | kinematics-conditioned (R=1.5, Ackermann) + curvature validator disposes infeasible turns | no path | — | — | — |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 4.076 | 12 | 0.163 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 4.222 | 81 | 0.253 |

_Path length is the sum of consecutive pose distances; lower is shorter (not always better — sampling planners trade optimality for speed, ARA\* trades it for an anytime bound). Pose count reflects densification granularity. "no path" means the planner raised an exception (e.g. the sampling budget did not connect start and goal within the scenario)._
