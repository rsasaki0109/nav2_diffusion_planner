# Classical planner comparison

Auto-generated by `nav2_planner_benchmarks` (`ros2 run nav2_planner_benchmarks planner_benchmark`). All planners run in-process against a live `nav2_costmap_2d::Costmap2DROS` (6x6 m, 0.05 m resolution, no Gazebo/GPU). Times are the median of 21 plans on the dev machine and are indicative only (absolute numbers vary with load); compare relative magnitudes and the path-length / shape columns.

Planners (all `nav2_core::GlobalPlanner` plugins absent from upstream Nav2 — eight classical plus four generative Mode B variants: analytic, learned (flow), learned (transformer), learned+classical fallback hybrid, and guided hybrid):

- **RRT*** — sampling (optimal)
- **RRT-Connect** — sampling (bidirectional)
- **PRM** — sampling (roadmap)
- **D* Lite** — incremental search
- **JPS** — grid A* speed-up
- **Lazy Theta*** — any-angle
- **ARA*** — anytime
- **Visibility graph** — geometric
- **Diffusion (Mode B, analytic)** — generative fan (propose + validate)
- **Diffusion (Mode B, learned)** — generative flow + costmap (propose + validate)
- **Diffusion (Mode B, transformer)** — generative transformer + costmap (fanned proposals)
- **Diffusion (Mode B, hybrid)** — generative propose + classical (JPS) fallback
- **Diffusion (Mode B, guided)** — generative-guided complete A* (tightly coupled)

> D\* Lite caches its goal-rooted search across calls, so its median reflects warm incremental replans (the first cold plan is slower). The others replan from scratch each call.

> **Diffusion (Mode B)** is the generative planner — a model *proposes* candidate paths and the deterministic validity layer *disposes* of colliding ones, keeping the shortest survivor. Five variants run here. **analytic** uses the built-in `FanPathModel` (a symmetric bowed fan, no ONNX); **learned (flow)** loads the curated costmap-conditioned flow model from `model_zoo` via `OnnxPathModel` (real ONNX inference); **learned (transformer)** loads the transformer Mode B model (attention over costmap tokens). All are pure generative, so unlike the search planners they are not complete: if no proposal threads the gap they report no path. The two learned models read the costmap and bias proposals to the open side, clearing *clear* and *side obstacle* but not the 2 m swing of *off-centre gap* or the S of *slalom* — so on this benchmark they behave alike. (Their difference is at the proposal level: the transformer *aims* its proposals at an off-centre slot where the flow model's 16-d CNN embedding cannot — a representational result that still does not thread the narrow footprint-validated slot here; see docs/generative_limits.md.) The **hybrid** variant keeps the learned proposal but adds a classical (JPS) fallback: when no proposal threads the map it hands off to a complete search, so it solves every scenario while still using the fast generative path on the easy ones. This is the propose/dispose split taken one step further — learned proposes, classical search disposes. The **hybrid (guided)** variant couples them tightly: it always runs a complete A* but discounts cell costs near the valid proposals, so the learned model *shapes* every route while the search guarantees completeness — note its cell-resolution pose counts even on easy maps, where the fallback hybrid instead returns the 12-pose generative path (see docs/generative_limits.md).

## Scenario: clear

Empty 6x6 m map, off-axis goal. Start (1, 1) -> goal (5, 5).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 5.658 | 10 | 1876.557 |
| RRT-Connect | sampling (bidirectional) | yes | 5.657 | 13 | 0.042 |
| PRM | sampling (roadmap) | yes | 6.175 | 17 | 34.527 |
| D* Lite | incremental search | yes | 5.657 | 81 | 0.545 |
| JPS | grid A* speed-up | yes | 5.657 | 81 | 0.789 |
| Lazy Theta* | any-angle | yes | 5.657 | 115 | 0.701 |
| ARA* | anytime | yes | 5.657 | 81 | 2.688 |
| Visibility graph | geometric | yes | 5.657 | 115 | 0.911 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 5.657 | 40 | 0.138 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | yes | 5.669 | 12 | 0.364 |
| Diffusion (Mode B, transformer) | generative transformer + costmap (fanned proposals) | yes | 5.665 | 12 | 0.219 |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 5.669 | 12 | 0.345 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 5.657 | 81 | 0.590 |

## Scenario: off-centre gap

Vertical wall with a gap well off the straight line. Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 5.084 | 12 | 764.774 |
| RRT-Connect | sampling (bidirectional) | yes | 6.359 | 14 | 0.104 |
| PRM | sampling (roadmap) | yes | 5.300 | 14 | 28.536 |
| D* Lite | incremental search | yes | 5.257 | 81 | 0.529 |
| JPS | grid A* speed-up | yes | 5.278 | 81 | 0.780 |
| Lazy Theta* | any-angle | yes | 5.073 | 104 | 8.067 |
| ARA* | anytime | yes | 5.278 | 81 | 16.001 |
| Visibility graph | geometric | yes | 5.095 | 105 | 0.905 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 5.282 | 40 | 0.106 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, transformer) | generative transformer + costmap (fanned proposals) | no path | — | — | — |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 5.278 | 81 | 1.165 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 5.257 | 81 | 1.209 |

## Scenario: slalom

Two staggered walls (gap low then high) forcing an S-shaped detour. Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 6.615 | 13 | 777.982 |
| RRT-Connect | sampling (bidirectional) | yes | 10.159 | 22 | 0.587 |
| PRM | sampling (roadmap) | yes | 8.058 | 23 | 37.619 |
| D* Lite | incremental search | yes | 6.785 | 107 | 0.595 |
| JPS | grid A* speed-up | yes | 6.829 | 107 | 0.822 |
| Lazy Theta* | any-angle | yes | 6.534 | 137 | 18.854 |
| ARA* | anytime | yes | 6.808 | 107 | 36.313 |
| Visibility graph | geometric | yes | 6.610 | 136 | 1.050 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | no path | — | — | — |
| Diffusion (Mode B, transformer) | generative transformer + costmap (fanned proposals) | no path | — | — | — |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 6.829 | 107 | 1.198 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 6.925 | 111 | 2.072 |

## Scenario: side obstacle

One-sided block across the straight line; a small detour to the open side clears it (matches the learned model's competence). Start (1.000, 3.000) -> goal (5.000, 3.000).

| Planner | Family | Success | Path length [m] | Poses | Median time [ms] |
|---|---|:-:|--:|--:|--:|
| RRT* | sampling (optimal) | yes | 4.028 | 9 | 851.133 |
| RRT-Connect | sampling (bidirectional) | yes | 5.395 | 12 | 0.060 |
| PRM | sampling (roadmap) | yes | 4.217 | 12 | 29.157 |
| D* Lite | incremental search | yes | 4.180 | 81 | 0.508 |
| JPS | grid A* speed-up | yes | 4.201 | 81 | 0.801 |
| Lazy Theta* | any-angle | yes | 4.027 | 82 | 1.258 |
| ARA* | anytime | yes | 4.201 | 81 | 3.024 |
| Visibility graph | geometric | yes | 4.029 | 82 | 0.887 |
| Diffusion (Mode B, analytic) | generative fan (propose + validate) | yes | 4.097 | 40 | 0.115 |
| Diffusion (Mode B, learned) | generative flow + costmap (propose + validate) | yes | 4.076 | 12 | 0.345 |
| Diffusion (Mode B, transformer) | generative transformer + costmap (fanned proposals) | yes | 5.175 | 12 | 0.222 |
| Diffusion (Mode B, hybrid) | generative propose + classical (JPS) fallback | yes | 4.076 | 12 | 0.345 |
| Diffusion (Mode B, guided) | generative-guided complete A* (tightly coupled) | yes | 4.222 | 81 | 0.541 |

_Path length is the sum of consecutive pose distances; lower is shorter (not always better — sampling planners trade optimality for speed, ARA\* trades it for an anytime bound). Pose count reflects densification granularity. "no path" means the planner raised an exception (e.g. the sampling budget did not connect start and goal within the scenario)._
