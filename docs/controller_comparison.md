# Local controller comparison (VFH+ / ND / learned Mode A)

Auto-generated by `nav2_planner_benchmarks` (`ros2 run nav2_planner_benchmarks controller_benchmark`). Each controller is rolled out closed-loop against a live `nav2_costmap_2d::Costmap2DROS` (6x6 m, 0.05 m) with a unicycle motion model (dt=0.1 s, up to 300 steps), following a straight global plan while reacting to obstacles. No Gazebo/GPU.

All are `nav2_core::Controller` plugins absent from upstream Nav2 (which ships DWB / MPPI / Regulated Pure Pursuit). Two are reactive: **VFH+** picks a free histogram valley by cost; **ND** selects a navigable gap and adds a safety deflection that centres the robot between close obstacles. The third is generative: **Diffusion (Mode A, learned)** loads the curated costmap-conditioned flow trajectory model from `model_zoo` via `OnnxTrajectoryModel` (real ONNX inference) — it *proposes* K trajectories that lean away from a one-sided obstacle (verified end-to-end in the `nav2_diffusion_onnx` test), a deterministic kinematic + footprint layer *disposes* of unsafe ones, and the controller drives the best. It is a small synthetic-trained research placeholder: it genuinely drives closed-loop (it traverses most of the *open* corridor) but does not reliably complete the full run here — the mature reactive controllers do. It is included to show a learned controller running in the same harness; the deterministic safety layer keeps it collision-free throughout (it stops rather than risk a collision). See its [model card](../model_zoo/diffusion_local/model_card.md).

## Scenario: open

No obstacles. Start (1, 3) -> goal (5, 3).

| Controller | Reached goal | Steps | Path length [m] | Min clearance [m] | Mean |dw| [rad] |
|---|:-:|--:|--:|--:|--:|
| VFH+ | yes | 75 | 3.705 | n/a | 0.000 |
| ND | yes | 75 | 3.705 | n/a | 0.000 |
| Diffusion (Mode A, learned) | timeout | 300 | 4.774 | n/a | 0.004 |

## Scenario: frontal obstacle

Lethal block mid-corridor, sides open. Start (1.000, 3.000) -> goal (5.000, 3.000).

| Controller | Reached goal | Steps | Path length [m] | Min clearance [m] | Mean |dw| [rad] |
|---|:-:|--:|--:|--:|--:|
| VFH+ | yes | 89 | 3.958 | 0.366 | 0.057 |
| ND | yes | 106 | 4.046 | 0.098 | 0.084 |
| Diffusion (Mode A, learned) | timeout | 300 | 1.072 | 0.766 | 0.004 |

## Scenario: side obstacle

Off-centre block overlapping the straight path; clearing it needs a detour to the open (lower) side. Start (1.000, 3.000) -> goal (5.000, 3.000).

| Controller | Reached goal | Steps | Path length [m] | Min clearance [m] | Mean |dw| [rad] |
|---|:-:|--:|--:|--:|--:|
| VFH+ | yes | 84 | 3.837 | 0.365 | 0.045 |
| ND | yes | 91 | 3.825 | 0.102 | 0.041 |
| Diffusion (Mode A, learned) | timeout | 300 | 0.894 | 0.844 | 0.002 |

## Scenario: corridor (off-centre start)

Walls at y=2.1 and y=3.9; robot starts at y=3.6, near the top wall. Start (1.000, 3.600) -> goal (5.000, 3.000).

| Controller | Reached goal | Steps | Path length [m] | Min clearance [m] | Mean |dw| [rad] | Mean |y-centre| [m] |
|---|:-:|--:|--:|--:|--:|--:|
| VFH+ | yes | 93 | 3.855 | 0.226 | 0.069 | 0.277 |
| ND | yes | 80 | 3.771 | 0.225 | 0.032 | 0.307 |
| Diffusion (Mode A, learned) | timeout | 300 | 0.000 | 0.226 | 0.000 | 0.600 |

_Min clearance is the smallest robot-to-obstacle distance over the run (higher = safer). Mean |dw| is the average step-to-step change in angular velocity (lower = smoother). Mean |y-centre| (corridor only) is how far from the corridor centreline the robot stayed (lower = better centred). The two reactive controllers (VFH+, ND) clear every scenario, trading berth (VFH+ tends to give wider clearance) against smoothness (ND tends to steer more smoothly). The learned Mode A controller is a small research model: it reads the costmap and drives closed-loop (traversing most of the *open* corridor) but does not reliably reach the goal box, so it reports "timeout" here while the mature reactive controllers reach. Its proposals are verified end-to-end in the `nav2_diffusion_onnx` test; in the loop the deterministic safety layer keeps it collision-free throughout. "timeout" means the goal was not reached within the step budget; "collision" means the robot entered a lethal cell._
